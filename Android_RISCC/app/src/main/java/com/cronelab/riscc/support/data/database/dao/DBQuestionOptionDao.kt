package com.cronelab.riscc.support.data.database.dao

/*

import androidx.room.Dao
import androidx.room.Query
import androidx.room.Transaction
import com.cronelab.diary.support.data.database.configuration.DatabaseConfigs
import com.cronelab.diary.support.data.database.table.questions.DBQuestionOption

@Dao
interface DBQuestionOptionDao : BaseDao<DBQuestionOption> {

    @Query("SELECT * FROM " + DatabaseConfigs.tbl_questionOption + " WHERE parentQuestionId = :questionId")
    fun getAllQuestionOption(questionId: String): List<DBQuestionOption>

    @Query("DELETE FROM " + DatabaseConfigs.tbl_questionOption)
    fun deleteAll()
}
*/
